import {VariableEditorCore} from '../core';
import {VariableEditorCoreProps} from '../types/editor';

export function VariableEditor(props: VariableEditorCoreProps) {
  return <VariableEditorCore {...props} />;
}
